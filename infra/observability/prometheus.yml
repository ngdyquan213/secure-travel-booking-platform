global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: secure-travel-app
    metrics_path: /metrics/prometheus
    static_configs:
      - targets:
          - app:8000
